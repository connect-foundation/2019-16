import React from "react";
import styled from "styled-components";

const StyledGroupDetailMain = styled.div``;

const groupDetailMain = () => {
  return <StyledGroupDetailMain></StyledGroupDetailMain>;
};

export default groupDetailMain;
